{
  "name": "aligned-evals",
  "version": "0.1.0",
  "private": true,
  "description": "Eval framework for Aligned plugin skills",
  "scripts": {
    "eval": "promptfoo eval",
    "eval:smoke": "promptfoo eval --filter-first-n 1 --no-write",
    "view": "promptfoo view",
    "test": "pytest tests/ -v",
    "test:scorer": "pytest tests/test_distinctness.py -v"
  },
  "devDependencies": {
    "promptfoo": "^0.100.0"
  }
}
